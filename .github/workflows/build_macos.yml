name: MacOs Build

on: [push]

jobs:
  build:
    runs-on: macos-latest
    steps:
    - uses: actions/checkout@v2
    - name: Make MacOs
      run: |
        echo 'echo -e "\x1b]0;TEST\007"'
        echo -e "\x1b]0;TEST\007"
#echo "Running ./blackhosts --help"
#./bin/blackhosts --help

# echo "Running sudo ./blackhosts fakenews-gambling -y"
# sudo ./blackhosts fakenews-gambling -y

# echo "Checking contents of /private/etc/hosts"
# cat /private/etc/hosts
